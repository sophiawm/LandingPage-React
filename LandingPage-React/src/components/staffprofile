import { staffList } from './staffdata.jsx'


/*function StaffProfile () {
return (
{staffList.forEach(staff => {
    return 
    <div className="staffProfile">
        <img src={staff.photoMenu} alt={staff.alt} />
        <div className="staffDetails">
            <h1>{staff.name}</h1>
            <h4>{staff.jobTitle}</h4>
            <p>{staff.description}<p>
            <div className="staffDetails">
                <img src="../images/linkedin.png" alt="LinkedIn Icon" />ç
                <button>Te llamamos</button>
            <div>
        </div>
    </div>
});}

);

}

export default StaffProfile */